global: # Configuración global de Prometheus
  scrape_interval: 15s

scrape_configs: # Configuración de los trabajos de scraping para recolectar métricas
  - job_name: "inventory-backend"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["host.docker.internal:8080"]
