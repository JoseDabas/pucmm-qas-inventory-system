import { CategoryList } from '../components/CategoryList';

/**
 * Página de Categorías. Solo compone: envuelve la tabla de categorías existente.
 */
export const CategoriesPage: React.FC = () => {
  return <CategoryList />;
};
