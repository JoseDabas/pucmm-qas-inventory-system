import { ProductList } from '../components/ProductList';

/**
 * Página de Inventario. Solo compone: envuelve la tabla de productos existente.
 */
export const InventoryPage: React.FC = () => {
  return <ProductList />;
};
